source "https://rubygems.org"

# Release automation. Run lanes with `bundle exec fastlane <platform> <lane>`.
# (No cocoapods gem: the Capacitor iOS project uses Swift Package Manager.)
gem "fastlane"
